plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.star_github.star_github"
    compileSdk = 35  // Update to Target SDK Version 35

    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.star_github.star_github"
        minSdk = 26
        targetSdk = 35
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    // 🟢 Move signingConfigs ABOVE buildTypes
    signingConfigs {
        release {
            storeFile file("../release-key.jks")
            storePassword System.getenv("storePassword") ?: "z10221607"
            keyAlias System.getenv("keyAlias") ?: "release-key"
            keyPassword System.getenv("keyPassword") ?: "z10221607"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release  // Now this is valid ✅
            shrinkResources true
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

flutter {
    source = "../.."
}